<%- include('../header') %>
<form method="POST" action="/publicacoes/editar/<%= categoria %>/<%= publicacao.id %>">
  <div class="form-group">
    <label for="name">Titulo</label>
    <input type="text" class="form-control" name="titulo" value="<%= publicacao.titulo %>" placeholder="Nome do titulo">
  </div>
  <div class="form-group">
    <label for="name">Conteúdo</label>
    <textarea class="form-control" name="conteudo" placeholder="Conteudo"><%= publicacao.conteudo %></textarea>
  </div>
  
  <button type="submit" class="btn btn-primary">Salvar</button>
</form>

<%- include('../footer') %>